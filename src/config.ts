// Supabase project credentials. Replace these with your project's values from
// Project Settings → API Keys in the Supabase dashboard.
//
// Use the new publishable key (`sb_publishable_...`), not the legacy anon JWT.
// The publishable key is safe to commit and ship in the browser: it only grants
// whatever Row Level Security policies allow. All data is gated behind
// `is_member()`, so a leaked publishable key still cannot read anything.
export const SUPABASE_URL = 'https://qspcogwwvpifopuwrhne.supabase.co';
export const SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_p9bzX2DL7L_nG2CuBsF4wQ_mV_04lVV';
