/** How long (ms) after typing stops a text is saved. Short enough that the
 *  app being killed loses a phrase at most; long enough that a save never
 *  lands mid-word. */
export const TEXT_SAVE_DELAY_MS = 1000;
