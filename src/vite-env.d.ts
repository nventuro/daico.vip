/** What this build is called: the day its commit was made and the commit's
 *  short hash, fixed at build time. Two devices showing different screens is
 *  usually two builds, and this is how anyone tells. */
declare const __APP_VERSION__: string;
